# Hostinger : document root imposé, créé par l'hébergeur, non versionné.
# Sur le serveur, ~/domains/surgicalhub.be/public_html/ contient le build
# React déployé (frontend/dist/) et l'entrée publique Symfony (backend/public/).
# Voir docs/production-hostinger.md.
/public_html/

# Dossier de cache stray créé à la racine (le vrai node_modules vit dans frontend/).
/node_modules/

# Captures d'écran de travail personnelles, hors dépôt applicatif.
/Visuel/
/Erruer templates.png
