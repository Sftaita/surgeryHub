<!DOCTYPE html>
<html lang="fr">
<head><meta charset="UTF-8"><style>
body { font-family: Arial, sans-serif; color: #333; font-size: 14px; line-height: 1.6; }
.container { max-width: 600px; margin: 0 auto; padding: 24px; }
.header { border-bottom: 2px solid #1565C0; padding-bottom: 12px; margin-bottom: 20px; }
.title { color: #1565C0; font-size: 18px; font-weight: bold; }
.footer { margin-top: 32px; font-size: 12px; color: #888; border-top: 1px solid #eee; padding-top: 12px; }
</style></head>
<body>
<div class="container">
  <div class="header">
    <div class="title">SurgicalHub — Planning</div>
  </div>

  <p>Bonjour {% if surgeon.firstname %}{{ surgeon.firstname }}{% else %}{{ surgeon.email }}{% endif %},</p>
  <p>Veuillez trouver ci-joint votre planning pour la période du <strong>{{ periodFrom|date('d/m/Y') }}</strong> au <strong>{{ periodTo|date('d/m/Y') }}</strong>.</p>
  {% if hasGlobal %}
  <p>Le planning global du site est également joint à titre informatif.</p>
  {% endif %}

  <div class="footer">SurgicalHub — document confidentiel</div>
</div>
</body>
</html>
